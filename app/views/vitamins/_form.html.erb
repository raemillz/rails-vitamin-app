<%= form_for(@vitamin) do |f| %>
  <% if @vitamin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vitamin.errors.count, "error") %> prohibited this vitamin from being saved:</h2>

      <ul>
      <% @vitamin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control" %></br>
    <%= f.label :capsule_amount, "How many capsules per serving?" %><br>
    <%= f.number_field :capsule_amount, class: "form-control" %>
  </div>

</br>

  <div class="field">
    Would you like to add or create and additional benefit?
    <%= f.fields_for :benefits do |benefit_fields| %>
      <%= benefit_fields.label :name %>
      <%= benefit_fields.text_field :name %>
    <% end %>

  </br>

    <%= f.collection_check_boxes :benefit_ids, Benefit.all, :id, :name %><br>
  </div>

  <div class="actions">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
